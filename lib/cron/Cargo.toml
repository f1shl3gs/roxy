[package]
name = "cron"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = [
    "serde"
]

[dependencies]
serde = { version = "1.0.156", default-features = false, optional = true }